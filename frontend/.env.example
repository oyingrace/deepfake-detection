# Production (choose one):
# 1. Leave unset — Vercel rewrites in vercel.json proxy /predict to Render (recommended)
# 2. Set to your Render URL — browser calls Render directly (also set ALLOWED_ORIGINS on Render)
VITE_API_URL=https://deepfake-server-hpuh.onrender.com
